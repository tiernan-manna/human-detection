# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
.venv/
env/

# Model weights (downloaded on first run, too large for git)
models/
*.pt
*.onnx

# Inference outputs
outputs/
sample_images/

# Recorded flight sessions (JPEGs + telemetry).
# Can contain PII from real delivery footage — never commit.
recordings/

# Fine-tuning run artifacts (weights, configs, plots).
# Weights are reproducible from scripts/finetune.py and shouldn't bloat the repo.
runs/

# OS / IDE
.DS_Store
.idea/
.vscode/
